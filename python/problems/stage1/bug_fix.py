# ============================================================
# STAGE 1: バグ修正問題
# ============================================================
# このプログラムにはバグが含まれている。
# バグを修正して正しく実行し、出力された値を答えとして入力せよ。
#
# 期待する動作:
#   get_target_file() がターゲットのファイル名（拡張子なし）を返す
# ============================================================


def get_target_file():
    name = "mast" + "er"
    return name + ".log"  # ← バグ: 拡張子なしのファイル名のみを返すべき


print(get_target_file())
